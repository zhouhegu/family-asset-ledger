{
  "name": "family-asset-ledger",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "description": "A lightweight, offline household asset ledger with English and Chinese interfaces.",
  "scripts": {
    "build:css": "tailwindcss -i styles/tailwind.css -o assets/tailwind.css --minify",
    "test": "node tests/ledger.test.cjs"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@tailwindcss/cli": "4.3.0",
    "tailwindcss": "4.3.0"
  }
}
